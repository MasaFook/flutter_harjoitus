import 'package:flutter/material.dart';

class App6 extends StatelessWidget{
  const App6({
    super.key,
});

 @override
  Widget build(BuildContext context) {
    return Scaffold(
        appBar: AppBar(
          title: const Text('App6')
        ));
  }
}